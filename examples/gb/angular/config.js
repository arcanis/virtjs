/*global require, module*/

require.config( {

    map : {
        '*' : {
            'virtjs' : '../../../libraries/base/index',
            'virtjs-gb' : '../../../libraries/architectures/gb/index'
        }
    }

} );
