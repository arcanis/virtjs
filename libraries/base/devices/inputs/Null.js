/*global define*/

define( [

    '../../utils/Class',

    '../../mixins/Emitter'

], function ( ClassUtil, EmitterMixin ) {

    return ClassUtil.extend( [

        EmitterMixin

    ], {

    } );

} );
